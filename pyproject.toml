[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "0.1.0"
description = "A simple todo list CLI"
requires-python = ">=3.8"
dependencies = [
    "rich",
    "argcomplete"
]

[project.scripts]
todo = "todo.main:main"
